import { Tabs } from "expo-router";

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: "#000",
        // Add other tab bar options here
      }}
    >
      {/* Your tab configurations go here */}
    </Tabs>
  );
}
